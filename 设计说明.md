# 磁盘扫描可视化工具 · 设计说明

> 版本：2026-09-19
> 对应源码：`scanner.py`（单文件实现）、`build.bat`（打包）、`test_scanner.py`（验证）

---

## 0. 总览

### 0.1 分层架构

```
┌──────────────────────────────────────────────────────────────┐
│ 界面层  MainWindow / ConfirmDeleteDialog / SelectionDialog    │
│         工具栏四行 + 树视图 + 右键菜单 + 多选窗口              │
├──────────────────────────────────────────────────────────────┤
│ 视图层  DiskScanDelegate（重写 paint，一行内分段着色）         │
│ 模型层  TreeModel（惰性 QAbstractItemModel，走可见子列表）     │
├──────────────────────────────────────────────────────────────┤
│ 筛选/排序层  NodeFilter / apply_filter / sort_tree             │
│              就地改可见性，保留树形结构                        │
├──────────────────────────────────────────────────────────────┤
│ 文件操作层  delete_permanently / send_to_recycle_bin           │
│              reveal_in_explorer / node_paths / remove_nodes    │
├──────────────────────────────────────────────────────────────┤
│ 数据层  Node（__slots__）/ ScanEngine（多线程 BFS + 后序聚合）  │
└──────────────────────────────────────────────────────────────┘
```

下层完全不依赖 Qt（只有文件操作层用到 `subprocess`/`ctypes`），因此筛选、排序、
删除、路径处理这些关键逻辑都能在无图形环境下单独测试。

### 0.2 功能索引

| 编号 | 功能 | 章节 |
|---|---|---|
| F1 | 删除文件（回收站 / 永久） | §1 |
| F2 | 一键复制完整路径 | §2 |
| F3 | 一键打开所在位置 | §3 |
| F4 | 左键拖拽拉选 + 右键多选 + 多选窗口 | §4 |
| F5 | 按体积 / 访问时间筛选（保留树形） | §5 |
| F6 | 多种排序方式（性能最优实现） | §6 |
| F7 | 一键重置筛选 | §7 |

### 0.3 关键常量

| 常量 | 默认值 | 含义 |
|---|---|---|
| `BULK_DELETE_THRESHOLD` | 50 | 超过此数量必须键入确认 |
| `LONG_PATH_WARN` | 240 | 超长路径提示阈值（Windows MAX_PATH=260） |
| `SELECTION_LIST_LIMIT` | 5000 | 多选窗口最多渲染的行数 |
| `MAX_REVEAL_DIRS` | 3 | 「打开所在位置」最多开几个资源管理器窗口 |
| `ConfirmDeleteDialog.PREVIEW_ROWS` | 200 | 确认框里预览多少条待删项 |

---

## 1. F1 · 删除文件

### 1.1 入口

三个入口，**行为完全一致**（都走同一个 `delete_nodes()`）：

| 入口 | 位置 |
|---|---|
| 工具栏按钮 | 第四行「删除」（红色文字，与其他操作区分） |
| 树右键菜单 | 「删除选中项…」 |
| 多选窗口 | 「删除选中项」 |

### 1.2 交互流程

```
点击删除
   │
   ├─ ① 取选中集合 ── 空 ──▶ 状态栏「没有选中任何项目，未执行删除」，结束（不弹框）
   │
   ├─ ② 存在性预检（对每项做 lexists）
   │      └─ 全部已不存在 ──▶ 弹提示「目标已不存在，建议重新扫描」，结束（零改动）
   │
   ├─ ③ 弹确认框（详见 1.3）
   │      └─ 用户取消 ──▶ 状态栏「已取消删除，未改动任何文件」，结束
   │
   ├─ ④ 执行
   │      回收站：SHFileOperationW + FOF_ALLOWUNDO
   │      永久：逐个 os.remove / shutil.rmtree
   │
   ├─ ⑤ 结果判定：重新对每个路径做 lexists
   │      —— 以「磁盘上是否还在」为准，而不是只看 API 返回码，
   │         因为批量操作可能部分成功（尤其被中途取消时）
   │
   ├─ ⑥ 同步内存树：摘掉已删节点 → 重新聚合体积 → 重排 → 重筛 → 刷新
   │
   └─ ⑦ 状态栏汇总 + 有失败项时弹明细
```

### 1.3 确认机制（三重）

| 层级 | 机制 | 目的 |
|---|---|---|
| 第 1 重 | 默认按钮是「取消」，且默认焦点在取消按钮；回车触发的是取消 | 防"条件反射式回车" |
| 第 2 重 | 默认方式是**移入回收站（可恢复）**；永久删除必须在确认框里显式切换单选按钮 | 默认路径永远可回退 |
| 第 3 重 | 删除量较大时（待删项数 > `BULK_DELETE_THRESHOLD`，**或**牵涉的文件总数超过该阈值——含目录内的子文件），必须手动键入**项目数量**，删除按钮才解禁 | 防"全选后手滑" |

确认框同时把风险信息全部前置展示：

- 项目数与构成（文件 N 个、目录 M 个）
- 若含目录：明确写出「会连同其下内容一并删除，合计牵涉 X 个文件、Y 个子目录」
- 其中有 K 项在磁盘上已不存在（将被跳过）
- 最长路径长度；超过 `LONG_PATH_WARN` 时提示改用永久删除
- 待删清单预览（前 200 项，含完整路径列）

### 1.4 实现要点

| 要点 | 做法 | 原因 |
|---|---|---|
| 回收站 | `SHFileOperationW`，`fFlags = FOF_ALLOWUNDO \| FOF_NOCONFIRMATION \| FOF_SILENT \| FOF_NOERRORUI \| FOF_WANTNUKEWARNING` | 我们自己已有确认框，用 `FOF_NOCONFIRMATION` 避免二次弹窗；`FOF_WANTNUKEWARNING` 作为兜底 |
| 参数构造 | `pFrom` 必须是**双 `\0` 结尾**的路径串（`build_double_null`） | 漏掉结尾分隔符会导致只处理第一个路径或直接失败 |
| 特殊字符 | 路径以 **列表** 形式交给系统 API，不经过 shell | `% & ^ ! ' " 空格` 全部无需转义，同时杜绝命令注入 |
| 只读文件 | 先 `os.chmod(S_IRWXU)` 摘掉只读位再删（单个文件与递归删除都覆盖） | Windows 上只读文件 `os.remove` 直接抛 `PermissionError` |
| 超长路径 | ≥240 字符时加 `\\?\` 前缀（`_long_path`） | 绕过 Win32 MAX_PATH 限制 |
| 部分失败 | `delete_permanently` 逐个处理，单个失败不影响其余，收集 `(路径, 错误)` | 批量删除时"一个坏文件拖垮整批"是常见痛点 |
| 内存树同步 | `remove_nodes()` 先把待删节点收进 `id` 集合，再对每个受影响父节点做一次过滤重建 | 逐个 `list.remove()` 是 O(n·k)，从十万子项里删一百项会卡；过滤重建是 O(n) |

### 1.5 边界情况处理

| 场景 | 期望行为 | 用户可见反馈 |
|---|---|---|
| 未选中任何文件 | 流程立即终止，**不弹任何对话框** | 状态栏「没有选中任何项目，未执行删除」 |
| 选中 1 个普通文件 | 弹确认框，无需键入 | 确认框；删除后状态栏「已删除 1 项」 |
| 选中多个文件（≤50） | 弹确认框，无需键入，清单全部列出 | 状态栏计数 |
| 选中大量文件（>50） | 弹确认框，**必须键入数量**才能点删除 | 输入框下方实时校验；数字不对按钮保持禁用 |
| 选中目录（含子树） | 确认框明确列出连带文件数；文件总数超阈值同样要求键入 | 红色风险行 |
| 选中项中部分已被删除/移动 | 只处理仍存在的部分，其余自动跳过 | 确认框提示「其中 K 项已不存在」 |
| 选中项**全部**已被删除/移动 | 不弹确认框，直接提示；**不做任何改动** | 提示框 + 状态栏 |
| 路径含特殊字符 | 原样传入，无需转义 | 无异常；操作正常完成 |
| 路径过长（≥240） | 回收站方式可能失败；确认框提前提示 | 确认框黄色提示行 |
| 目标文件夹无权限 | 单文件失败被收集，其余继续 | 结束后弹「部分删除失败」并列出前 8 条路径 |
| 文件被其他程序占用 | 同上（`PermissionError`） | 同上，提示文案里说明常见原因 |
| 删除操作被中途取消 | 以实际结果为准：已删的从树里摘掉，未删的保留 | 状态栏追加「操作被中途取消」 |
| 删除提交被系统拒绝 | 不修改内存树，保持界面与磁盘一致 | 弹「删除失败」并给出错误码 |
| 删除后 | 树自动重算体积、重排、重筛 | 状态栏计数更新，父级体积同步变化 |

---

## 2. F2 · 一键复制完整路径

### 2.1 交互流程

```
选中若干项
   │
   ├─ 空选中 ──▶ 状态栏「没有选中任何项目，未复制」，返回 0
   │
   └─ 非空 ──▶ 取 node.path（绝对路径）→ 去重 → 以 \n 连接 → 写入剪贴板
                    │
                    └─▶ 状态栏「已复制 N 条完整路径到剪贴板」
```

三个入口：工具栏「复制完整路径」、树右键菜单、多选窗口（「复制全部路径」/
「复制选中行路径」）。

### 2.2 实现

- 使用 `QApplication.clipboard().setText()`，多项目以换行分隔
- 复制的是树节点的 `node.path`，即**扫描时拼出的绝对路径**
- `node_paths()` 会去重（多选父目录与其子项时避免重复行）并保持首次出现顺序

### 2.3 边界情况

| 场景 | 行为 |
|---|---|
| 未选中 | 不写剪贴板（**不覆盖你原有的剪贴板内容**），状态栏提示 |
| 选中 1 项 | 剪贴板就是该路径，无多余换行 |
| 选中多个 | 每行一个，末行不带换行 |
| 选中父目录与其子项 | 去重后不出现重复路径 |
| 路径含空格 / 中文 / 特殊字符 | 原样写入，不加引号（便于直接粘进终端或资源管理器地址栏） |
| 选中大量项 | 一次性写入，无长度限制问题 |

---

## 3. F3 · 一键打开文件所在位置

### 3.1 交互流程

```
选中若干项
   │
   ├─ 空选中 ──▶ 状态栏提示，返回 0
   │
   ├─ 全部已不存在 ──▶ 弹「无法定位」，结束
   │
   └─ 按所在目录分组（同目录只开一次窗口）
        └─▶ 依次调用 explorer.exe
             └─▶ 状态栏汇总：已打开 N 个位置 / 跳过 K 项 / 未打开 M 个目录 / 失败原因
```

### 3.2 退化策略

| 目标状态 | 行为 |
|---|---|
| 普通文件存在 | `explorer.exe /select,<完整路径>` → 打开父目录并**选中**该文件 |
| 目标是目录 | 直接打开该目录（不加 `/select`） |
| 文件已不存在，但父目录还在 | 打开父目录（用户至少能确认"文件确实没了"） |
| 文件与父目录都不存在 | 返回失败，提示「文件已被删除或移动，且所在目录也不存在」 |

### 3.3 多选时的窗口控制

- 按**所在目录**分组去重，同一目录只开一个窗口（选中同目录下 50 个文件不会弹 50 个窗口）
- 最多打开 `MAX_REVEAL_DIRS`（默认 3）个不同目录，其余在状态栏说明「另有 N 个不同目录未打开，避免弹出过多窗口」

### 3.4 实现要点

- `subprocess.Popen(["explorer.exe", arg])` —— **列表传参，不经过 shell**，因此路径里的 `& % ^ ! ' "` 都不会被 shell 解释
- 不等待进程结束（`explorer.exe` 会把请求转交给已有进程后立刻退出）

### 3.5 边界情况

| 场景 | 行为 |
|---|---|
| 未选中 | 状态栏提示，不启动任何进程 |
| 文件已被删除或移动 | 退化为打开父目录；父目录也没了则明确报错 |
| 无权限访问目标文件夹 | `explorer.exe` 会自行弹出系统权限提示，程序不阻塞；定位调用本身仍返回成功 |
| 路径含特殊字符 | 无转义问题（列表传参） |
| 路径过长 | 交给 explorer 处理（Win10+ 支持长路径）；极端情况下 explorer 自身报错 |
| 网络路径不可达 | explorer 自行提示；程序不阻塞 |
| 选中多个不同目录 | 同目录合并，最多 3 个窗口，其余在状态栏说明 |
| 非 Windows 平台 | 返回「该功能仅在 Windows 上可用」 |

---

## 4. F4 · 多选交互与多选窗口

### 4.1 左键拖拽拉选

树视图配置：

| 设置 | 值 | 作用 |
|---|---|---|
| `selectionMode` | `ExtendedSelection` | 既支持拖拽连续拉选，也支持 Ctrl/Shift 点选 |
| `dragDropMode` | `NoDragDrop` | **关键**：不关掉拖放的话，左键拖拽会被解释成"拖动条目"而不是"拉选" |
| `selectionBehavior` | `SelectRows` | 整行选中，避免只选中单列 |
| `autoExpandDelay` | `-1` | 拖拽拉选时经过的目录不自动展开，避免视野乱跳 |

### 4.2 右键加入选择

右键点击一个**未被选中**的条目时，把它**加入**已有选择，而不是替换掉整个选择
（已在选择内的条目右键不会改变选择）。随后弹出上下文菜单：

```
已选中 N 项          ← 只读标题，实时反映数量
────────────────
打开多选窗口…
复制完整路径
打开所在位置
────────────────
删除选中项…
```

### 4.3 多选窗口

| 项 | 设计 |
|---|---|
| 模态性 | **非模态**，不挡住主窗口继续操作 |
| 列 | 名称 / 完整路径 / 类型 / 大小 / 访问时间 |
| 操作按钮 | 复制全部路径、复制选中行路径、打开所在位置、刷新、删除选中项、关闭 |
| 语义 | **打开时刻的快照**，不随主窗口选择变化而跳动（避免你正在窗口里操作时列表被换掉）；需要同步时点「刷新」 |
| 删除/定位的作用范围 | 若表格中有高亮行 → 针对高亮行；若无高亮 → 针对**全部**选中项 |
| 大集合 | 表格最多渲染 `SELECTION_LIST_LIMIT`（5000）行并给出说明；**复制/打开/删除始终作用于完整集合**，不受表格行数限制 |
| 空集合 | 所有操作按钮置灰，表头显示「共选中 0 项」；不弹窗、不报错 |

### 4.4 边界情况

| 场景 | 行为 |
|---|---|
| 未选中任何文件就点「查看选中」 | 状态栏提示，**不打开空窗口** |
| 选中数量在窗口打开后变化 | 窗口保持快照；点「刷新」或重新打开即同步 |
| 选中大量项（如 10 万） | 表格只渲染 5000 行 + 说明；复制路径仍导出全部 10 万条 |
| 在窗口内删除后 | 窗口列表自动同步为"删除后剩下的项" |
| 主窗口关闭 | 窗口是主窗口的子对象，随之关闭 |

---

## 5. F5 · 筛选（保留树形结构）

### 5.1 交互

第三行工具栏：`筛选 · 大小 [不限] ~ [不限] GB`、`距今 [不限] ~ [不限] 天`。
输入框取到最小值时显示「不限」，表示该侧不限制。输入变化后**防抖 320ms** 再计算。

### 5.2 可见性判定

- **判据**：文件用自身体积与时间；目录用**聚合体积**（与界面显示值一致）
- **自身命中** → 可见
- **目录自身不命中，但下级有命中项** → 默认保留为"路径"，保证树形不散架
- 取消勾选「保留上级路径」→ 严格模式：自身不命中就整枝隐藏
  （此时藏在不符合条件目录里的命中文件也会看不见，这是严格模式的预期语义）

### 5.3 边界情况

| 场景 | 行为 |
|---|---|
| 条件全为「不限」 | 视为未筛选，所有节点可见 |
| 只有下限 / 只有上限 | 单侧限制，另一侧不限 |
| 上下限写反（min > max） | 无任何节点命中，树为空；重置即可恢复 |
| 目录自身命中但子项全被过滤 | 目录仍显示，但不画展开箭头（`hasChildren` 为假） |
| 未来时间戳（时钟偏差） | 天数按 0 处理，不会被 `days_max` 误排除 |
| 筛选后 | 展开状态尽量保留；选择会被清空（筛选后原选中项可能已不可见） |

---

## 6. F6 · 排序（性能最优实现）

### 6.1 排序方式

| 选项 | 键 |
|---|---|
| 大小 ↓ / ↑ | `n.size` |
| 名称 A→Z / Z→A | `n.name.lower()` |
| 时间（新→旧 / 旧→新） | `metric_ts(n, 当前指标)` |
| 目录优先（同类按大小 ↓） | `(0 if n.is_dir else 1, -n.size)` |
| 扫描顺序（不排序） | — |

排序**递归作用于全树每个目录**的子节点。

### 6.2 性能设计

**（1）必须用 `key=`，不能用 `cmp_to_key`。**

`list.sort(key=...)` 由 Timsort 排序，每个元素**只调用一次** key 函数；而
`functools.cmp_to_key` 每次比较都要回到 Python 层，调用次数是 O(n·log n)。
实测数据（16 万节点，400 目录 × 400 项，两边都先打乱）：

| 实现 | 回调次数 | 耗时 |
|---|---|---|
| `sort(key=...)` | 160,000 | **0.066 s** |
| `cmp_to_key` | 1,177,187 | 0.400 s |

回调多 **7.4 倍**，耗时慢 **6 倍**。

**（2）只在必要时排序。** 用 `(排序模式, 时间指标)` 作为签名，签名不变就跳过——
改筛选条件不会触发重排。

**（3）Timsort 自适应。** 已有序输入接近 O(n)（实测全树 0.036s vs 乱序 0.072s）。

### 6.3 边界情况

| 场景 | 行为 |
|---|---|
| 时间排序 + 切换时间指标 | 时间排序依赖指标，指标变化会触发重排（签名里包含指标） |
| 同名项 | Timsort 稳定排序，保持扫描顺序 |
| 目录与文件混排 | 按所选方式统一比较；「目录优先」显式分组 |
| 选择「扫描顺序」 | 不做任何排序（`sort_tree` 返回 False），也不回写 row（保持扫描时的下标） |
| 排序后 | `row` 下标同步回写（模型的 `parent()` 依赖它） |

---

## 7. F7 · 一键重置

「重置筛选」按钮：
- 一次性清空四个区间输入（0 = 不限），此过程**屏蔽信号**避免触发四次刷新
- 然后做一次强制刷新：取消筛选、恢复完整树
- **排序方式保持不变**
- 展开状态尽量保留

---

## 8. 边界情况总表（对照需求逐项）

| 需求点名的场景 | 覆盖位置 | 处理方式 | 对应验证项 |
|---|---|---|---|
| 未选中任何文件 | §1.5 / §2.3 / §3.5 / §4.4 | 一律提前返回，不弹框、不写剪贴板、不起进程 | `未选中时删除返回 None` 等 3 项 |
| 选中多个文件 | §1.5 / §2.3 | 逐个处理；确认框列出清单 | `确认后删除成功返回 1` |
| 选中大量文件 | §1.3 | 必须键入数量；多选窗口仅渲染 5000 行 | `超过阈值数量需要键入` |
| 文件已被删除或移动 | §1.2 ② / §3.2 | 预检分离；全部不存在则零改动退出；定位退化为打开父目录 | `split_existing 分离存在/不存在`、`文件已消失：退化为打开父目录` |
| 路径含特殊字符 | §1.4 / §3.4 | 列表传参不经过 shell，无需转义 | `特殊字符路径原样传入` |
| 路径过长 | §1.4 | 提示 + 永久删除加 `\\?\` 前缀 | `超长路径自动加 \\?\ 前缀` |
| 无权限访问目标文件夹 | §1.5 / §3.5 | 单文件失败被收集、其余继续；explorer 自行提示 | `永久删除：不存在的项进入失败清单` |
| 删除失败 | §1.2 ⑤⑥ | 以磁盘实际状态为准；失败项弹明细；内存树不做错误修改 | `永久删除：不存在的项进入失败清单` |
| 删除中途取消 | §1.2 ⑤ | 已删的摘掉、未删的保留，状态栏标注"被中途取消" | `移入回收站：未被中途取消` |

---

## 9. 性能设计汇总

| 环节 | 复杂度 | 关键做法 |
|---|---|---|
| 扫描 | O(N) | 多线程 BFS；每个目录的计数本地累加后再一次性加锁 |
| 体积聚合 | O(N) | 迭代式后序 DFS（不用递归，避免深目录爆栈） |
| 排序 | O(Σ kᵢ log kᵢ) | Timsort + `key=`；签名未变则跳过 |
| 筛选 | O(N) | 两遍线性遍历；**只有确有隐藏子项的目录才分配额外列表**，无筛选时零额外内存 |
| 节点摘除 | O(父节点子项数) | `id` 集合 + 一次过滤重建，而非逐个 `list.remove()` |
| 展开状态恢复 | O(已展开数) | 用 `expanded/collapsed` 信号维护集合，而非每次全树扫 `isExpanded()` |
| 输入响应 | — | 筛选输入防抖 320ms，避免每敲一键就全树遍历 |

---

## 10. 验证覆盖

`test_scanner.py` 为无头（offscreen）自动化验证脚本，覆盖 A–K 共 11 组，
**当前 285 项断言全部通过**：

| 组 | 内容 |
|---|---|
| A | 纯逻辑边界（时间/体积分档、格式化） |
| B | 扫描引擎（真实目录、聚合、排序、无权限跳过、路径不存在） |
| C | 树模型（index/parent/rowCount/role 行为） |
| D | 委托绘制（**逐像素**断言两段颜色与顺序） |
| E | 委托健壮性（极窄行、极端时间戳） |
| F | 主窗口端到端（跨线程信号、模型挂载、控件联动、展开保持） |
| G | 指标切换与节点上限 |
| H | 行内样式（色标 / 带数值） |
| I | 筛选（判据边界、树形保留、严格模式、模型自洽、重置） |
| J | 排序（8 种结果、递归性、**算法性能基准**） |
| K | 文件操作（纯逻辑 / 真实删除与定位 / 界面联动 / 确认框） |
| L | 扫描进度模型（卷根判定、三阶段合成、单调性、速率与剩余时间、真实扫描端到端） |

其中值得说明的验证方式：

- **删除**：在临时目录里创建真实文件与只读文件，跑真实删除并断言"文件确实没了"；
  递归删除断言目录消失但父目录仍在
- **回收站**：真实调用一次 `SHFileOperationW`（对象是本次新建的临时文件），
  断言调用成功且文件从原位置消失
- **打开所在位置**：**拦下 `subprocess.Popen`** 并校验参数，
  避免测试期间弹出真实资源管理器窗口；同时验证三种退化路径
- **剪贴板**：测试前保存、测试后还原原剪贴板内容
- **确认框**：不弹真实窗口，断言默认方式、默认按钮、键入校验的开启/禁用时机

关键场景实测输出（节选）：

```
[OK] 永久删除：只读文件也被删掉
[OK] 永久删除：目录被递归删除          （父目录仍在）
[OK] 移入回收站：调用成功 / 文件已从原位置消失 / 未被中途取消
[OK] 特殊字符路径原样传入（无 shell 转义问题）
[OK] 取消删除后文件仍在 / 状态栏说明未改动
[OK] 目标不存在时给出明确提示（非静默）
[OK] 根节点体积已重算 -> 18 -> 14     （删文件后）
[OK] 根体积扣掉了整棵子树 -> 11        （删目录后）
[OK] 未选中时删除返回 None / 复制返回 0 / 定位返回 0
```

---

## 11. 扫描进度条设计

### 11.1 原问题

原来的进度条是 `setRange(0, 0)` 的不确定进度：只有一个来回滚动的方块，
既不反映真实进展，也给不出任何数字，体验上"很无聊"。

### 11.2 为什么不能简单地算个百分比

**扫描前无法知道总条目数** —— 要数一遍就得先遍历一遍。实测"只计数不建对象"的
预遍历耗时是完整扫描的 **90%~125%**，两遍法总耗时约 **1.9~2.25 倍**，不可接受。

**而基于 BFS 前沿的估算在真实文件系统上完全不可用。** 实测 `C:\Windows\System32`：

| 真实进度 | 估算 A | 估算 B |
|---|---|---|
| 20.7% | 54.7% | 15.3% |
| 46.0% | 39.1% | 8.8% |
| 60.0% | 99.3% | 98.8% |
| 64.6% | **0.4%** | **0.1%** |

最后一行的崩塌原因：System32 下存在含 **10,725 个子目录**的目录，待处理目录数
从 2 瞬间涨到 10,725，任何以它为分母的估算都会崩。真实文件系统的树极度不平衡，
这条路线不通。

### 11.3 最终方案：按"能否拿到真实分母"自适应

| 扫描目标 | 进度语义 | 分母来源 |
|---|---|---|
| **卷根**（`C:\` 这类） | **确定进度**：已发现字节 ÷ 卷已用字节 | `shutil.disk_usage().used` —— **真实分母** |
| **子目录** | **不确定进度**（不编数字）+ 密集实时数字 | 无可信分母，诚实显示 |
| 聚合体积 / 排序 | **确定进度** | 已发现的节点总数（此时已知） |

三阶段在整条进度条上的分配：枚举 0~80%，聚合 80~95%，排序 95~100%。
这样即使大数据量下聚合排序要几秒，进度条也仍在推进，不会"卡在 100%"。

对应实现：`is_volume_root()` 判定用哪种算法，`estimate_scan_percent()` 是纯函数，
`ProgressTracker` 负责单调化、速率平滑与剩余时间估算。

### 11.4 实测：卷扫描的贴合度

`C:\` 全盘扫描，1,342,782 个条目 / 203.6 GB / 18.3 秒：

| 时间进度 | 进度条显示 | 偏差 |
|---|---|---|
| 0.8% | 8.7% | +7.9 |
| 14.1% | 34.0% | +20.0 |
| 27.4% | 51.4% | +24.0 |
| 48.0% | 78.1% | +30.0 |
| 62.5% | 86.2% | +23.7 |
| 84.7% | 99.0% | +14.3 |
| 92.9% | 99.0% | +6.1 |
| 99.7% | 99.9% | +0.2 |

**平均绝对偏差 18.6%，全程单调递增，永不倒退、永不卡死。**

偏差的来源是本质性的：扫描成本与**条目数**成正比，而体积覆盖衡量的是**数据量**；
前 30% 的字节集中在少量顶层大文件里，所以体积进度会跑在前面。要消除这个偏差
只能做预遍历（慢 1.9 倍）。这里选择了"诚实地展示数据覆盖进度"而不是"编一个像时间的数字"。

### 11.5 实时反馈（这是"无聊"的真正解药）

状态栏右侧常驻**进度条 + 一行实时数字**（120ms 刷新）：

```
[■■■■■■■■■■■■□□□□□□] 78.1% · 按体积   128.4 GB / 196.8 GB   目录 12,345   文件 456,789   跳过 1,234   8,421 项/秒   已用 00:47   剩余约 01:12
```

状态栏左侧显示**依据与当前目录**：

```
按体积 ▸ C:\Windows\System32\drivers
```

即使百分比在某段时间推进变慢，条目数、字节数、速率与计时也一直在变，
所以界面上永远不会出现"看起来卡住了"的状态。

### 11.6 边界情况

| 场景 | 行为 |
|---|---|
| 扫描卷根 | 确定进度；分母为卷已用字节，随容量自动缩放（128G 盘与 2T 盘表现一致） |
| 扫描子目录 | 不确定进度 + 实时数字；**不编造百分比**，依据栏标注「按实况」 |
| 拿不到卷用量（网络盘/权限不足） | 退化为不确定进度，不显示除法结果 |
| 已发现字节超过卷已用字节（压缩/稀疏/硬链接） | 封顶 99%，收尾时跳到 100%，不显示 103% |
| 估算分母波动 | `ProgressTracker` 强制单调不减，绝不倒退 |
| 触达节点上限 | 状态栏追加「[已达节点上限，正在收尾]」 |
| 聚合 / 排序阶段 | 切成精确进度；这两个阶段的总量是已知的 |
| 用户手动停止 | 立即隐藏进度条并清空数字，状态栏说明「结果未聚合」 |
| 扫描完成 | 进度条补满到 100% 再撤下，避免"停在 97%"的错觉 |
| 速率尚不可算（刚开始） | 不显示速率与剩余时间，避免出现 `inf` 或 `0 项/秒` |

---

## 12. 已知限制与设计取舍

| 项 | 说明 |
|---|---|
| 回收站 API 的路径长度 | `SHFileOperationW` 受老 Win32 MAX_PATH(260) 限制；超长路径请用永久删除（确认框会提前提示） |
| 「访问时间」可能不准 | NTFS 可关闭访问时间更新（`NtfsDisableLastAccessUpdate`），此时读到 0 会回退用修改时间；且文件系统**不存在"访问次数"字段** |
| 永久删除不进回收站 | 这是用户显式选择的行为，确认框已用不同措辞区分二者 |
| 筛选后选择被清空 | 原选中项在筛选后可能已不可见；展开状态会保留，选择不保留 |
| 多选窗口是快照 | 不随主窗口选择实时跳动，避免操作中列表被换；提供「刷新」按钮 |
| 删除后不自动重新扫描磁盘 | 仅同步内存树中"我们自己删掉的"节点；若在软件外部改动文件，需要重新扫描 |
| 符号链接 / 目录联接 | 扫描阶段即跳过（避免死循环与重复统计），因此不出现在树里 |
| 无权限目录 | 扫描时跳过并计数，不出现在树里，因此也无法从界面删除其中内容 |
