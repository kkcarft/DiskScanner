# 默认按文本处理并统一用 LF 入库，避免不同平台克隆后换行不一致
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd.exe 对 for /f、goto 等语法解析会出错
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不要做任何换行或编码转换
*.png binary
*.jpg binary
*.ico binary
*.exe binary
*.zip binary
